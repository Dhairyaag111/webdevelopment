creat an anonymous function that returns the square of the number. use it inside the main function to print the square of 5
write a anonymous function to make a calcualtor
write a anonyous function to make calculator and check whether its neg,pos,=zero




let func_add = function(a, b){
    return a + b;
}
let func_sub = function(a, b){
    return a - b;
}
let func_mul = function(a, b){
    return a * b;
}
let func_div = function(a, b){
    return a/b
}
function nextfunction(a, b, c) {
    if (c=='+'){
        if(func_add>0){
            console.log(`The sum of ${a} and ${b} is ${func_add(a,b)}.Its is a positive value.`)
        }
        else if(func_add<0){
            console.log(`The sum of ${a} and ${b} is ${func_add(a,b)}. It is a negative value.`)
        }
        else if(func_add ==0){
            console.log("The sum of ${a} and ${b} is equal to zero")
        }
    } 
    else if (c=='-'){
       if(func_sub>0){
            console.log(`The difference of ${a} and ${b} is ${func_sub(a,b)}.Its is a positive value.`)
        }
        else if(func_sub<0){
            console.log(`The diffenence of ${a} and ${b} is ${func_sub(a,b)}. It is a negative value.`)
        }
        else if(func_sub ==0){
            console.log("The difference of ${a} and ${b} is equal to zero")
        }
    } 
    else if (c=='*'){
        if(func_mul>0){
            console.log(`The product of ${a} and ${b} is ${func_mul(a,b)}.Its is a positive value.`)
        }
        else if(func_mul<0){
            console.log(`The product of ${a} and ${b} is ${func_mul(a,b)}. It is a negative value.`)
        }
        else if(func_mul ==0){
            console.log("The product of ${a} and ${b} is equal to zero")
        }
    } 
    else if(c=='/'){
        if(func_div>0){
            console.log(`The division of ${a} and ${b} is ${func_div(a,b)}.Its is a positive value.`)
        }
        else if(func_div<0){
            console.log(`The division of ${a} and ${b} is ${func_div(a,b)}. It is a negative value.`)
        }
        else if(func_div==0){
            console.log("The division of ${a} and ${b} is equal to zero")
        }
    }
    else{
        console.log("The operator you enter is incorrect")
    }
}
let a = Number(prompt("Enter the first number: "))
let b = Number(prompt("Enter the second number: "))
let c = prompt("Enter the operator (+, -, *, /): ")

nextfunction(a,b,c)